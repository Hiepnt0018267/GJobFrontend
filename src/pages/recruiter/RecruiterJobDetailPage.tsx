import { AlertCircle, ArrowLeft, Banknote, BriefcaseBusiness, Building2, CalendarDays, FilePenLine, MapPin, UsersRound } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import JobStatusBadge from '../../components/recruiter/JobStatusBadge'
import RecruiterHeader from '../../components/recruiter/RecruiterHeader'
import { useDataRefreshVersion } from '../../hooks/useDataRefreshVersion'
import { recruiterJobService } from '../../services/recruiterJobService'
import type { RecruiterJob } from '../../types/job'
import { recruiterJobErrorMessage } from '../../utils/apiError'
import { employmentLabel, formatSalary, industryLabel, levelLabel, workModeLabel } from '../../utils/jobDisplay'

export default function RecruiterJobDetailPage() {
  const { id = '' } = useParams(); const refreshVersion = useDataRefreshVersion(); const [job, setJob] = useState<RecruiterJob | null>(null); const [error, setError] = useState<string | null>(null)
  useEffect(() => { let active = true; recruiterJobService.getMyJob(id).then((value) => { if (active) setJob(value) }).catch((requestError) => { if (active) setError(recruiterJobErrorMessage(requestError)) }); return () => { active = false } }, [id, refreshVersion])
  return <div className="min-h-screen bg-slate-50"><RecruiterHeader /><main className="mx-auto max-w-5xl px-4 py-8 sm:px-6"><Link to="/recruiter/jobs" className="inline-flex items-center gap-1.5 text-sm font-semibold text-slate-500 hover:text-blue-600"><ArrowLeft size={16} />Quản lý tin tuyển dụng</Link>{!job && !error && <div className="mt-6 h-96 animate-pulse rounded-2xl bg-slate-200" />}{error && <div role="alert" className="mt-6 rounded-2xl bg-white p-10 text-center ring-1 ring-slate-200"><AlertCircle className="mx-auto text-red-500" size={30} /><p className="mt-4 font-semibold text-slate-900">{error}</p></div>}{job && <><section className="mt-6 rounded-2xl bg-slate-900 p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div><JobStatusBadge status={job.status} /><h1 className="mt-4 text-2xl font-bold tracking-tight sm:text-3xl">{job.title}</h1><p className="mt-2 flex items-center gap-2 text-sm text-slate-300"><Building2 size={16} />{job.company_name}</p></div>{job.status !== 'CLOSED' && <Link to={`/recruiter/jobs/${job.id}/edit`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-slate-900 hover:bg-slate-100"><FilePenLine size={16} />Chỉnh sửa</Link>}</div><div className="mt-7 grid gap-3 text-sm text-slate-200 sm:grid-cols-2 lg:grid-cols-4"><span className="flex items-center gap-2"><MapPin size={16} />{job.location || 'Chưa cập nhật'}</span><span className="flex items-center gap-2"><BriefcaseBusiness size={16} />{employmentLabel(job.employment_type)}</span><span className="flex items-center gap-2"><UsersRound size={16} />{job.vacancies} vị trí</span><span className="flex items-center gap-2"><CalendarDays size={16} />{new Intl.DateTimeFormat('vi-VN').format(new Date(job.created_at))}</span></div></section><section className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_300px]"><div className="space-y-6 rounded-2xl bg-white p-6 ring-1 ring-slate-200 sm:p-8"><div><h2 className="text-lg font-bold text-slate-900">Mô tả công việc</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-7 text-slate-600">{job.description}</p></div><div className="border-t border-slate-100 pt-6"><h2 className="text-lg font-bold text-slate-900">Yêu cầu ứng viên</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-7 text-slate-600">{job.requirements || 'Chưa cập nhật'}</p></div><div className="border-t border-slate-100 pt-6"><h2 className="text-lg font-bold text-slate-900">Quyền lợi</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-7 text-slate-600">{job.benefits || 'Chưa cập nhật'}</p></div>{job.skills && job.skills.length > 0 && <div className="border-t border-slate-100 pt-6"><h2 className="text-lg font-bold text-slate-900">Kỹ năng</h2><div className="mt-3 flex flex-wrap gap-2">{job.skills.map((skill) => <span key={skill} className="rounded-lg bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{skill}</span>)}</div></div>}</div><aside className="h-fit rounded-2xl bg-white p-6 ring-1 ring-slate-200"><h2 className="font-bold text-slate-900">Tóm tắt</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-500">Ngành nghề</dt><dd className="mt-1 font-semibold text-slate-900">{industryLabel(job.industry)}</dd></div><div><dt className="text-slate-500">Cấp bậc</dt><dd className="mt-1 font-semibold text-slate-900">{levelLabel(job.level)}</dd></div><div><dt className="text-slate-500">Cách thức làm việc</dt><dd className="mt-1 font-semibold text-slate-900">{workModeLabel(job.work_mode)}</dd></div><div><dt className="flex items-center gap-1.5 text-slate-500"><Banknote size={15} />Mức lương</dt><dd className="mt-1 font-semibold text-emerald-700">{formatSalary(job)}</dd></div></dl></aside></section></>}</main></div>
}
