import { AlertCircle, Building2, Edit3, Globe2, MapPin, Phone, UserRound, Users } from 'lucide-react'
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { recruiterService } from '../../services/recruiterService'
import type { RecruiterProfile } from '../../types/recruiter'
import { recruiterErrorMessage } from '../../utils/apiError'
import { useDataRefreshVersion } from '../../hooks/useDataRefreshVersion'

const missing = (value: string | number | null) => value === null || value === '' ? <span className="italic text-slate-400">Chưa cập nhật</span> : value
function Skeleton() { return <div className="animate-pulse space-y-5"><div className="h-24 rounded-2xl bg-slate-200" /><div className="h-56 rounded-2xl bg-slate-200" /></div> }

export default function RecruiterProfilePage() {
  const refreshVersion = useDataRefreshVersion(); const [profile, setProfile] = useState<RecruiterProfile | null>(null); const [error, setError] = useState<string | null>(null); const [retry, setRetry] = useState(0)
  useEffect(() => { let active = true; recruiterService.getMyRecruiterProfile().then((data) => { if (active) setProfile(data) }).catch((requestError) => { if (active) setError(recruiterErrorMessage(requestError)) }); return () => { active = false } }, [refreshVersion, retry])
  return <div className="min-h-screen bg-slate-50"><main className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8"><nav className="mb-6 flex gap-2 text-sm text-slate-500"><Link to="/recruiter" className="hover:text-blue-600">Tổng quan</Link><span>/</span><span className="text-slate-700">Hồ sơ công ty</span></nav>{!profile && !error && <Skeleton />}{error && <div role="alert" className="rounded-2xl bg-white p-8 text-center shadow-sm ring-1 ring-slate-200"><AlertCircle className="mx-auto text-red-500" size={32} /><h1 className="mt-4 text-xl font-bold text-slate-900">Không thể tải hồ sơ</h1><p className="mt-2 text-sm text-slate-500">{error}</p><button onClick={() => { setError(null); setRetry((value) => value + 1) }} className="mt-5 rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Thử lại</button></div>}{profile && <><section className="rounded-2xl bg-slate-900 p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center overflow-hidden rounded-2xl bg-blue-600 text-xl font-bold">{profile.company_logo_url ? <img src={profile.company_logo_url} alt="Logo công ty" className="h-full w-full object-cover" /> : (profile.company_name ?? profile.full_name).charAt(0).toUpperCase()}</div><div><h1 className="text-2xl font-bold">{profile.company_name || 'Hồ sơ công ty'}</h1><p className="mt-1 text-sm text-slate-300">{profile.full_name} · Nhà tuyển dụng</p></div></div><Link to="/recruiter/profile/edit" className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-800 hover:bg-slate-100"><Edit3 size={16} />Cập nhật hồ sơ</Link></div></section><section className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_280px]"><div className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-lg font-bold text-slate-900">Thông tin doanh nghiệp</h2><dl className="mt-6 grid gap-5 sm:grid-cols-2"><div><dt className="flex items-center gap-2 text-sm font-semibold text-slate-600"><Building2 size={15} />Tên công ty</dt><dd className="mt-1.5 text-sm text-slate-900">{missing(profile.company_name)}</dd></div><div><dt className="flex items-center gap-2 text-sm font-semibold text-slate-600"><Globe2 size={15} />Website</dt><dd className="mt-1.5 text-sm text-slate-900">{profile.company_website ? <a href={profile.company_website} target="_blank" rel="noreferrer" className="text-blue-600 hover:underline">{profile.company_website}</a> : missing(null)}</dd></div><div><dt className="flex items-center gap-2 text-sm font-semibold text-slate-600"><MapPin size={15} />Địa chỉ</dt><dd className="mt-1.5 text-sm text-slate-900">{missing(profile.company_address)}</dd></div><div><dt className="flex items-center gap-2 text-sm font-semibold text-slate-600"><Users size={15} />Quy mô</dt><dd className="mt-1.5 text-sm text-slate-900">{profile.company_size ? `${profile.company_size} nhân sự` : missing(null)}</dd></div><div><dt className="flex items-center gap-2 text-sm font-semibold text-slate-600"><Building2 size={15} />Ngành nghề</dt><dd className="mt-1.5 text-sm text-slate-900">{missing(profile.industry)}</dd></div></dl><div className="mt-7 border-t border-slate-100 pt-6"><h3 className="text-sm font-semibold text-slate-700">Giới thiệu công ty</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-7 text-slate-600">{missing(profile.company_description)}</p></div></div><aside className="h-fit rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h2 className="text-base font-bold text-slate-900">Thông tin liên hệ</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="flex items-center gap-2 text-slate-500"><UserRound size={15} />Người liên hệ</dt><dd className="mt-1 font-medium text-slate-900">{profile.full_name}</dd></div><div><dt className="flex items-center gap-2 text-slate-500"><Phone size={15} />Điện thoại</dt><dd className="mt-1 font-medium text-slate-900">{missing(profile.phone)}</dd></div><div><dt className="text-slate-500">Email</dt><dd className="mt-1 break-all font-medium text-slate-900">{profile.email}</dd></div></dl></aside></section></>}</main></div>
}
