/* eslint-disable react-hooks/set-state-in-effect */
import { AlertCircle, ArrowLeft, CalendarClock, FileText, RefreshCw, Star } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import { Link, useLocation, useParams } from 'react-router-dom'
import AdminHeader from '../../components/admin/AdminHeader'
import AdminCVPreviewReadOnly from '../../components/admin/audit/AdminCVPreviewReadOnly'
import AdminUserAvatar from '../../components/admin/users/AdminUserAvatar'
import { useDataRefreshVersion } from '../../hooks/useDataRefreshVersion'
import { adminAuditService } from '../../services/adminAuditService'
import type { AdminAuditCVDetail } from '../../types/adminAudit'
import { cvFileTypeLabel, formatFileSize } from '../../types/cv'
import { adminAuditErrorMessage, getApiErrorStatus } from '../../utils/apiError'
import { formatJobTimestamp } from '../../utils/jobDisplay'

const getReturnTo = (state: unknown) => state && typeof state === 'object' && 'returnTo' in state && typeof state.returnTo === 'string' ? state.returnTo : '/admin/cvs'

export default function AdminCandidateCVDetailPage() {
  const { id } = useParams<{ id: string }>()
  const location = useLocation()
  const refreshVersion = useDataRefreshVersion()
  const [cv, setCV] = useState<AdminAuditCVDetail | null>(null)
  const [error, setError] = useState<string | null>(null)
  const [isNotFound, setIsNotFound] = useState(false)
  const [isLoading, setIsLoading] = useState(true)
  const [requestVersion, setRequestVersion] = useState(0)
  const keepVisible = useRef(false)
  const returnTo = getReturnTo(location.state)
  useEffect(() => { let active = true; const preserveData = keepVisible.current; keepVisible.current = false; if (!preserveData) setIsLoading(true); setError(null); setIsNotFound(false); if (!id) { setError('CV ứng viên không còn tồn tại.'); setIsNotFound(true); setIsLoading(false); return () => { active = false } } adminAuditService.getCandidateCV(id).then((response) => { if (active) setCV(response) }).catch((requestError: unknown) => { if (!active) return; setIsNotFound(getApiErrorStatus(requestError) === 404); setError(adminAuditErrorMessage(requestError, 'detail', 'cv')) }).finally(() => { if (active) setIsLoading(false) }); return () => { active = false } }, [id, refreshVersion, requestVersion])
  const reload = (preserveData = false) => { keepVisible.current = preserveData; setRequestVersion((version) => version + 1) }
  const isUploaded = cv?.source_type === 'UPLOADED' || !cv?.template

  return <div className="min-h-screen bg-slate-50"><AdminHeader /><main className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><Link to={returnTo} className="inline-flex items-center gap-1.5 text-sm font-semibold text-slate-500 hover:text-blue-700"><ArrowLeft size={16} />Quay lại danh sách</Link>{isLoading && !cv && <div className="mt-6 space-y-6" aria-busy="true"><div className="h-44 animate-pulse rounded-2xl bg-slate-200" /><div className="h-[620px] animate-pulse rounded-2xl bg-slate-200" /></div>}{!isLoading && error && !cv && <section role="alert" className="mt-6 rounded-2xl bg-white px-6 py-14 text-center shadow-sm ring-1 ring-slate-200"><AlertCircle className="mx-auto text-red-600" size={34} /><h1 className="mt-4 text-xl font-bold text-slate-950">{isNotFound ? 'CV ứng viên không còn tồn tại.' : 'Không thể tải chi tiết CV.'}</h1><p className="mt-2 text-sm text-slate-500">{error}</p>{!isNotFound && <button type="button" onClick={() => reload()} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><RefreshCw size={16} />Thử lại</button>}</section>}{cv && <div className="mt-6 space-y-6"><header className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200 sm:p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><div className="flex flex-wrap items-center gap-3"><h1 className="break-words text-2xl font-bold tracking-tight text-slate-950">{cv.title}</h1>{cv.is_default && <span className="inline-flex items-center gap-1 rounded-full bg-amber-50 px-2.5 py-1 text-xs font-bold text-amber-800"><Star size={13} />CV mặc định</span>}</div><p className="mt-2 text-sm leading-6 text-slate-600">Bản xem chỉ đọc của CV ứng viên.</p></div><span className="inline-flex items-center gap-1.5 text-sm text-slate-500"><CalendarClock size={16} />Cập nhật {formatJobTimestamp(cv.updated_at)}</span></div></header><section className="grid gap-5 md:grid-cols-2"><article className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-semibold text-slate-500">Chủ sở hữu</p><div className="mt-4 flex items-center gap-3"><AdminUserAvatar name={cv.candidate.full_name} url={cv.candidate.avatar_url} size="lg" /><div className="min-w-0"><Link to={`/admin/users/${cv.candidate_id}`} className="block break-words font-bold text-slate-950 hover:text-blue-700">{cv.candidate.full_name}</Link><p className="mt-1 break-all text-sm text-slate-500">{cv.candidate.email}</p></div></div><Link to={`/admin/users/${cv.candidate_id}`} className="mt-5 inline-flex text-sm font-semibold text-blue-700 hover:text-blue-900">Mở hồ sơ ứng viên</Link></article><article className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-semibold text-slate-500">{isUploaded ? 'Tệp CV' : 'Mẫu CV đang tham chiếu'}</p>{isUploaded ? <><div className="mt-4 flex items-center gap-2 text-lg font-bold text-slate-950"><FileText size={20} className="text-blue-600" />CV tải lên</div><p className="mt-2 break-words text-sm text-slate-600">{cv.original_filename || cv.title}</p><p className="mt-2 text-sm text-slate-500">{cvFileTypeLabel(cv.mime_type)} · {formatFileSize(cv.file_size)}</p></> : <><p className="mt-4 break-words text-lg font-bold text-slate-950">{cv.template?.name}</p><div className="mt-2 flex flex-wrap gap-2"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{cv.template?.layout_key}</span><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${cv.template?.is_active ? 'bg-emerald-50 text-emerald-800' : 'bg-slate-100 text-slate-700'}`}>{cv.template?.is_active ? 'Đang hoạt động' : 'Ngừng hoạt động'}</span></div></>}</article></section><section aria-labelledby="cv-audit-preview"><div className="mb-4"><h2 id="cv-audit-preview" className="text-xl font-bold text-slate-950">Nội dung CV</h2><p className="mt-1 text-sm text-slate-500">{isUploaded ? 'Thông tin tệp CV ứng viên đã tải lên.' : 'Hiển thị theo layout và theme của mẫu được API trả về.'}</p></div><AdminCVPreviewReadOnly cv={cv} /></section></div>}</main></div>
}
