/**
 * Compatibility placeholder while legacy page modules are migrated to
 * AdminLayout. Navigation and account controls now live exclusively in that
 * persistent route shell.
 */
export default function AdminHeader() {
  return null
}
